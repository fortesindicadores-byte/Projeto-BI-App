name: KM Jun/PIR Inspect

on:
  workflow_dispatch:
    inputs:
      vig:
        description: 'Vigência MM/AAAA (vazio = última com dado)'
        required: false
        default: ''

jobs:
  inspect:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: claude/epic-edison-ae21sz
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Junho sem empurrada e PIR com sinal invertido
        env:
          VIG: ${{ inputs.vig }}
        run: node scripts/km-jun-pir-inspect.mjs
